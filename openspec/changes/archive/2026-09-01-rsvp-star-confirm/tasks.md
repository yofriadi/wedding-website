# Tasks: rsvp-star-confirm

## 1. StarButton Astro component

- [x] 1.1 Create `apps/web/src/components/StarButton.astro`: static button markup — button root (paints the conic-gradient rim) → `::after` opaque interior plate → `<span class="star-btn__label">` with `<slot />`. Props: `type` (default `submit`), `class`, `disabled`, `aria-disabled`, `data-*` passthrough. No starfield SVG, no hairline border, no light element (design D2). Scoped styles declare the `--star-btn-*` tokens ON THE BUTTON ROOT — component-owned, self-contained, never overridden by the section (design D5): two polarity tokens as the dark-page fallback (`--star-btn-surface: #fafafa`, `--star-btn-ink: #0a0a0a`) with every painted color equal to one of them (plate/sweep = surface, label/rim base = ink) + a `@media (prefers-color-scheme: light)` block that swaps ONLY those two.
- [x] 1.2 The rim, in CSS only (design D3): `@property --star-btn-angle` (`syntax: "<angle>"`, `initial-value: 0deg`) + a `star-btn-spin` keyframe to `360deg`, run `linear`/`infinite` over `--star-btn-duration` (3s); root background is `conic-gradient(from var(--star-btn-angle), base, sweep, base)` clipped by `border-radius` + `overflow: hidden`; `::after` inset by `--star-btn-rim-width` (5px) with radius `calc(--star-btn-radius - --star-btn-rim-width)` covers everything but the band. **No script, no measurement, no observers** — the previous rAF/`offset-path` implementation is deleted, which also deletes its resize, visibility, and border/label-sync concerns. A disabled button keeps rotating at full opacity (spec: the effect is state, not affordance); only `cursor` changes.
- [x] 1.3 Degradation (design D3 matrix): `prefers-reduced-motion: reduce` → `animation: none` (the band renders at its initial angle, label unaffected), declared after the disabled rule so reduce wins; missing `@property` support → the same static band with no feature query (an untyped property cannot interpolate); no-JS → nothing to degrade; `@media (forced-colors: active)` → rim replaced by `outline: 2px solid ButtonBorder` at `outline-offset: -2px` (same box), plate `ButtonFace`, label `ButtonText`.
- [x] 1.4 Verify in isolation AND regenerate the committed test fixture: `src/pages/star-button-fixture.astro` renders a fresh + a disabled button standalone; `node scripts/make-star-button-fixture.mjs` captures the served markup + scoped CSS into `apps/web/tests/fixtures/star-button.html` (consumed by task 3.2; the script now asserts the component CSS survived the dev-runtime strip instead of requiring an inlined script). Checked: dark + light emulation screenshots, reduced motion (static), disabled button (rotating, undimmed), forced-colors, no layout shift, text announced as the accessible name.

## 2. RSVP section rework

- [x] 2.1 Rewrite the invitee branch of `apps/web/src/components/RsvpSection.astro` markup: display name line + one `<StarButton type="submit">Confirm Reservation</StarButton>` (label copy: "Reservation Confirmed" + check when `rsvpAttending === true`, rendered `disabled` + `aria-disabled="true"` with the rim still rotating). Delete the Attending/Decline toggle pair, the party-size stepper block, and the `data-attending`/`data-party-size`/`data-max` staging attributes; keep the error/note `<p>`s (note becomes `aria-live="polite"`), form id, and the anonymous guidance branch untouched.
- [x] 2.2 Slim the form script: submit handler POSTs the fixed body `{ attending: true, partySize: 1 }` to `/api/rsvp`; on 200 flip the button to the confirmed state (label swap to "Reservation Confirmed", `disabled = true`, `aria-disabled`), **move focus to the button**, announce via the live-region note ("Your reservation is confirmed."), and trigger the counter poll (existing behavior). Remove the choice/stepper state, `syncUI` toggling, and step listeners; keep the 400/503/network error-note handling (button stays enabled). Replace the stale 400 copy "That response isn't within your party size — please adjust and retry." (references the removed stepper) with "Couldn't save your RSVP. Please try again." Delete now-dead code (`data-rsvp-choice`/`data-rsvp-step`/`data-rsvp-party` queries, `attending`/`partySize` locals, `max`).
- [x] 2.3 Leftover-contract cleanup (design D4a): drop `maxPartySize` and `rsvpPartySize` from `RsvpSection.astro`'s Props interface; stop passing `inviteMaxPartySize`/`inviteRsvpPartySize` at the `index.astro` call site (`rsvpAttending` stays — it drives confirmed-vs-fresh). Type-check proves nothing else consumed them.
- [x] 2.4 Theme the section (design D5): declare the `--rsvp-bg/--rsvp-fg/--rsvp-muted` tokens at `:root` (dark fallbacks) + `@media (prefers-color-scheme: light)` override in `index.astro`'s EXISTING `<style>` block — the file that owns `#rsvp-section` (its `--loading-bg` is the shipped precedent); `RsvpSection.astro`'s scoped styles only consume the inherited vars. Replace `#rsvp-section`'s `bg-neutral-950` class with `background-color: var(--rsvp-bg)`; retarget the counter digits (`text-neutral-50` → var), "guests confirmed", "RSVP for NAME", note/error colors.
- [x] 2.5 FAQ copy reconciliation (`apps/web/src/components/WeddingFAQ.astro`, copy-only): plus-one answer's "you'll confirm attendance for your party" → simple attendance confirmation ("you'll confirm your attendance"); capacity sentence untouched; deadline entry untouched.

## 3. Verify

- [x] 3.1 Playwright section-level additions to `apps/web/tests/rsvp.spec.ts` (real page, anonymous — counter + copy render for everyone; `page.emulateMedia({ colorScheme })` per `venue-map-interactions.spec.ts`): light-scheme flips the section background to white and counter digits/copy to near-black; existing anonymous/counter tests pass unchanged.
- [x] 3.2 Playwright button-level additions in a new `apps/web/tests/star-button.spec.ts` against the committed fixture `tests/fixtures/star-button.html` (the real page renders no button for anonymous visitors and no-seed means no invite row — this is the only executable coverage of the star-button capability): dark/light polarity inversion (computed plate/sweep/base/label colors, each asserted equal to a polarity token in BOTH themes), rim band geometry (plate inset = rim width, concentric radii, opaque plate, `overflow: hidden`, zero border width), the angle advancing on a 3s linear infinite loop and no script shipping at all, reduced-motion + forced-colors static presentations, no-JS parity, rotation continuing undimmed while disabled, single accessible name.
- [x] 3.3 curl authed verification (per `rsvp-live-count` 2.6 convention): the fixed body `{ attending: true, partySize: 1 }` — fresh invite inserts, declined invite upserts to attending, 200 echoes `{ attending: true, partySize: 1 }`; count increments by exactly 1; anonymous POST with garbage body still 404. (Deep contract cases — responded_at survival, busy→503 — are NOT re-verified: the endpoint is untouched and `rsvp-live-count` 2.6 already covered them; this proves only the new always-valid body.)
- [x] 3.4 `pnpm check-types` passes; `pnpm build` + smoke: dark and light emulation visual pass on the section (seam with adjacent dark sections accepted), disabled-confirmed rendering for a pre-confirmed invite (manual — needs a seeded invite).
