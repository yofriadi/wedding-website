# public-wall

Show guest stories and wishes to public (non-invitee) visitors once real posts exist; retire the invite-only teaser tiles at that point. Photo files become publicly fetchable.

**Archive order**: archive `story-rail-mocks` FIRST, then this change — its `story-rail-mocks` and `guest-photos` deltas MODIFY requirements that only exist in the base specs once `story-rail-mocks` archives. Statements in that change about the public rail "returning to teaser/demo content" or making zero submissions requests are superseded by this change and annotated accordingly there.
