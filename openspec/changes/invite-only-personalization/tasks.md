# Tasks: invite-only-personalization

## 1. Deployment: bare VM + Caddy + node standalone + local SQLite

- [x] 1.1 Simplify `astro.config.mjs`: `@astrojs/node` standalone is the only adapter; remove the Cloudflare/LOCAL_DEV switch. NOTE: `pnpm dev` still runs the Vite dev server — NOT the same runtime as prod. Prod parity is only testable via `pnpm build && node dist/server/entry.mjs`.
- [x] 1.2 Local SQLite wiring: make `DATABASE_AUTH_TOKEN` optional in `packages/env/src/server.ts`; `packages/db/src/index.ts` omits `authToken` when absent; change `packages/db/drizzle.config.ts` `dialect` from `"turso"` to `"sqlite"`; `DATABASE_URL` points at a file (e.g. `file:/srv/wedding/wedding.db`); apply schema via `pnpm db:migrate` (committed migrations), `db:push` only for disposable local DBs. DATA STORY: confirm no invites exist in Turso yet (fresh start) — if any do, export them into the local file before cutover or they are lost. ALSO: update stale Turso workflow docs — `packages/db/package.json` and root README still tell developers to run `turso dev`.
- [ ] 1.3 VM prep: confirm public IP + pay-per-traffic billing with 30–50 Mbps cap; confirm security group allows inbound 80/443 (and 22); confirm system disk ≥20 GiB. **Add the DNS A record pointing the domain at the VM's public IP** — Caddy auto-TLS cannot issue a cert until DNS resolves.
- [ ] 1.4 Install + configure Caddy on Ubuntu 26.04: site block `your-domain { reverse_proxy 127.0.0.1:4321; @sub path /api/submissions; request_body @sub { max_size 60MB } }`; auto-TLS verified; confirm response headers pass through (see 2.3). Long-lived caching for fingerprinted assets: `header /_astro/* Cache-Control "public, max-age=31536000, immutable"`. ALSO cache the heavy unfingerprinted `public/` media (`/*.jpg`, the MP3) — they're stable all season; without a long-lived header they revalidate every visit and inflate metered egress (review 6, item 15). HTML stays `no-store`. The `request_body` limit is the pre-buffer upload ceiling (review 4, blocker 2).
- [ ] 1.5 Process management: systemd unit running the node server with EXPLICIT env: `User` (service account), `WorkingDirectory=/srv/wedding/app`, `EnvironmentFile` with `NODE_ENV=production` (required — apps/web/src/pages/[id].ts emits a non-`Secure` cookie when NODE_ENV=development), `HOST=127.0.0.1`, `PORT=4321`, `DATABASE_URL=file:/srv/wedding/wedding.db`, `PHOTO_STORAGE_DIR=/srv/wedding/photos`, `INVITE_ADMIN_TOKEN`, `INVITE_COOKIE_DAYS=75` (default 30 < release→wedding span Sep 19–Oct 17; ~75 dissolves L2 for free), and `CORS_ORIGIN`. Create + chown the DB dir and photo dir for the service account. Restart on boot + crash.
- [x] 1.6 Neutralize the Cloudflare deploy path: remove/disable root `deploy`/`destroy` scripts; remove now-unused Cloudflare deps from `apps/web` and root; AND remove `@wedding-website/infra` from `packages/env/package.json` devDependencies (it references the retired package — removing infra without this breaks `pnpm install` for the whole workspace); drop `alchemy`/`wrangler`/`@cloudflare/workers-types` from the `pnpm-workspace.yaml` catalog; delete `packages/infra`; fix dead root scripts `dev:local` (`LOCAL_DEV=true`, dead after 1.1) and `db:local` (`turso dev`, dead after 1.2).
- [x] 1.7 Smoke-test the standalone server BEFORE deploy: from `apps/web`, `pnpm build && node dist/server/entry.mjs` (the path is `apps/web/dist/...`, NOT repo-root `dist/`); confirm the page renders, the greeting path works, `no-store` is present, static assets (images/MP3) serve 200, and the cookie is `Secure` under NODE_ENV=production. [Verified locally via curl: page renders, greeting anonymous/invited, `no-store` on homepage + `/:id` 302 + `/api/invite/me`, `Set-Cookie … Secure`, static assets 200. Caddy pass-through check deferred to deploy.]
- [ ] 1.8 Backup from day one (do NOT defer to guest-submissions): nightly off-box backup of the SQLite DB (`sqlite3 .backup`) as soon as the VM is live. Decide BEFORE implementation: destination (object storage with scoped credentials — a VM cron CANNOT push to a laptop behind NAT, so either object-storage-push or laptop-pulls), tool, credentials, retention, owner, failure notification. Make it incremental. ADD a one-line restore check (restore the `.backup` to a scratch file + open it) — an untested backup isn't one. guest-submissions extends this job to photos.
- [ ] 1.9 DEPLOY PATH (review 6, B2 — nothing currently gets code to the VM): install Node + pnpm on the VM; clone or copy source; `pnpm install` ON THE BOX (native `@libsql/*` binaries are platform-specific — rsyncing a macOS `node_modules` or macOS-built `dist/` fails at runtime); `pnpm --filter web build`; apply migrations against the PRODUCTION `DATABASE_URL` (`pnpm run db:migrate` — must exit 0 BEFORE any server start/restart; libSQL auto-creates an empty file and the site looks healthy while every invite query fails `no such table`); then run `dist/server/entry.mjs`. Use a versioned release dir + `current` symlink so the Sep 19 release has a one-command rollback. ~~Also make `CORS_ORIGIN` optional or delete it in `packages/env`~~ [done — now optional].

## 2. Greeting (server-rendered)

- [x] 2.1 In `index.astro` frontmatter: read `ww_invite_id` cookie → lookup `invites` (local SQLite) → pass `displayName` (or null) into the overlay markup. NOTE: `index.astro` currently imports NO server modules — this adds `import { db }` + `import { env }` to a page frontmatter for the first time; fine, just deliberate. [The `env` import turned out unnecessary — only `db` is used.]
- [x] 2.2 Render greeting element above the shimmer phrases in `#loading-screen`; group-center the pair; anonymous/null renders an empty element (public markup identical to today); Astro auto-escapes the name
- [x] 2.3 Set the header explicitly in `index.astro`: `Astro.response.headers.set("Cache-Control", "no-store")` (all visitors); verify the header survives the Caddy reverse proxy (Caddy passes upstream headers by default — confirm, do not strip) [Caddy pass-through deferred to deploy.]
- [x] 2.4 Create `apps/web/src/pages/api/invite/me.ts` per `invite-session` spec (uniform 404, `no-store`, read-only) — contract for later changes, not consumed by the greeting
- [x] 2.5 Cache-safety sweep for invite endpoints: add `Cache-Control: no-store` to the `/:id` 302 response (it carries `Set-Cookie` and bumps seen-metrics — a cached redirect would skip both) and to the admin `/api/admin/:token/invites` success/error responses (same unconsidered-header class as the round-4 photo bug)
- [x] 2.6 Verify: anonymous (no name, phrases centered alone); invited cold load (name at first paint); invited warm-cache load (identical, no floor needed); unknown/malformed cookie (empty greeting, no error); `/api/invite/me` scripted curls (uniform 404 shapes, `no-store`, no metrics delta); homepage `no-store` present through Caddy; `/:id` 302 carries `no-store` and still sets the cookie + bumps metrics [All verified locally via curl except Caddy pass-through, deferred to deploy.]

## 3. Story teaser

- [x] 3.1 Remove the "Add Story" input tile from `index.astro` (file-input + label block)
- [ ] 3.2 Couple supplies up to three teaser images at `apps/web/public/teasers/story-teaser-{1,2,3}.webp` (owner: couple; must land before the Sep 19 release — without them the rail ships with no teaser)
- [x] 3.3 Render teaser tiles via an explicit frontmatter array of asset paths (no directory scan): map each existing path to a plain `<img>` (112×200, non-interactive, not a `StoryViewer`); skip missing entries [Exists-check probes both `public/` (dev) and `dist/client/` (build) so missing assets degrade gracefully.]
- [x] 3.4 Verify: no file input anywhere in the DOM; teaser tiles render for each provided asset; rail makes no network requests beyond static images [Verified locally with two placeholder assets; placeholders removed after test.]

## 4. Close-out

- [x] 4.1 Confirm anonymous homepage HTML contains no invite display names and an empty greeting element; invited HTML contains exactly one
- [x] 4.2 Confirm invite IDs appear in no URL other than `/:id` (grep client scripts)
- [x] 4.3 Diff-review anonymous HTML vs. pre-change output: no unintended content differences (greeting element, removed add-tile, teaser tiles excepted). Note: byte comparison will fail on scoped-style hashes — review the diff, don't diff bytes [Reviewed `git diff apps/web/src/pages/index.astro`: only greeting wrapper, add-tile removal, teaser map.]
- [x] 4.4 Correct stale site dates in the three files that show them: `HeroZoom.astro` (September 24, 2026), `TimelineScroll.astro` (24 September 2026), `WeddingFAQ.astro` (August 1 RSVP deadline) → real date Oct 10, 2026; confirm with couple before changing FAQ RSVP copy [HeroZoom + TimelineScroll corrected to Oct 10, 2026; FAQ RSVP deadline awaits couple confirmation.]
