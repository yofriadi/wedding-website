# Tasks: group-invitations

## 1. Database schema & baseline migration

- [x] 1.0 **Recorded owner decision (2026-09-28, D5 option A): the guest data in `packages/db/local.db` is disposable** — the DB will be deleted and recreated from the regenerated baseline under the authorized replacement procedure in `ops/README.md`. Pre-flight before deleting: confirm no OTHER environment holds data that must survive (check for stray DB files / WAL siblings and that no production instance is deployed from this repo), and note that `apps/web/var/photos` uploads become orphaned once their `guest_photos` rows are gone — delete or archive them in the same step (task 1.5). Deployment consequence to state explicitly: `DEPLOY.md` runs `migrate.mjs` on every container start, so any Docker volume initialized on the OLD baseline will be REFUSED by the regenerated one — if such a deployment exists, the same owner-authorized replacement must be run there
- [x] 1.1 In `packages/db/src/schema/invites.ts` add `parentId` (nullable self-FK via lazy `(): AnySQLiteColumn => invites.id` callback), `type` (text, `notNull().default("individual")`), `maxMembers` (nullable integer), `invites_parent_id_idx`, and the four CHECK constraints from design D1 (`invites_type_chk`, `invites_group_shape_chk`, `invites_member_shape_chk`, `invites_max_members_range_chk`). Add `check`, `index`, and the `AnySQLiteColumn` TYPE to the `drizzle-orm/sqlite-core` import, and `sql` to a `drizzle-orm` import — `AnySQLiteColumn` is NOT exported from top-level `drizzle-orm` (verified against drizzle-orm@0.45.2 typings: the root re-exports only dialect-agnostic modules; the type lives in `drizzle-orm/sqlite-core`), so importing it from `drizzle-orm` fails `pnpm check-types`
- [x] 1.2 Regenerate the baseline from empty: **delete `packages/db/src/migrations/**` first** (all `.sql` and `meta/`), then `pnpm db:generate -- --name initial`. Verify the emitted artifacts are exactly `0000_initial.sql`, `meta/0000_snapshot.json`, and `_journal.json` with `entries[0].tag === "0000_initial"` (if drizzle-kit emits a different slug, rename the SQL + snapshot and fix the journal tag). Confirm no `0001_*` file or second journal entry remains — `database-baseline.spec.ts` asserts a single entry and `migrate.mjs` hashes every entry
- [ ] 1.3 Verify with the configured migrator against a disposable SQLite file: fresh apply succeeds, second run is a no-op, `PRAGMA foreign_key_check`/`integrity_check` pass, the four CHECKs reject bad rows (group without `max_members`, out-of-range quota, group with `parent_id`, individual with `max_members`) — and a CHECK violation surfaces as a CHECK constraint error, not misclassified as an id collision. Encode these as automated assertions in `apps/web/tests/database-baseline.spec.ts` (the repo's home for baseline scenarios). Also assert the self-FK deletion backstop for the spec's "Group with members cannot be deleted" scenario: insert a group + one member, assert `DELETE FROM invites WHERE id = <group>` rejects with a FK constraint error, and that deleting a MEMBERLESS group succeeds (design D1's mechanical backstop to the no-delete ops rule)
- [ ] 1.4 Confirm `rsvps` and `guest_photos` schemas are untouched. There is no existing invites column-list assertion to update: `database-baseline.spec.ts` asserts only the `guest_photos` columns (`toEqual(["id","invite_id","key","created_at"])`) and `rsvp-api.spec.ts:48-50` only checks the invites DDL for fragments / `not.toContain("max_party_size")`. ADD a new `invites` column-list assertion to `database-baseline.spec.ts` covering `parent_id`, `type`, and `max_members`
- [x] 1.5 Replace the local database per the authorized procedure in `ops/README.md` (D5 option A — owner confirmed the data is disposable): stop the local Astro server and any AVIF job, delete `packages/db/local.db`, `local.db-wal`, `local.db-shm`, re-run `pnpm db:migrate`, and verify the regenerated baseline applied cleanly (`invites` has the three new columns; zero rows). Also clear or archive `apps/web/var/photos` (its `guest_photos` rows no longer exist). FIRST take a required one-off snapshot copy of `local.db` + `-wal`/`-shm` and the `var/photos` tree — that is the rollback set `ops/README.md`'s replacement procedure requires (precedent: the 2026-09-16 handoff's safety snapshot), and it costs nothing. NOTE the deviation: `ops/README.md` prefers pointing at a NEW empty database path over in-place deletion; the owner's disposability ruling authorizes in-place deletion here, so record that deviation in the change handoff / PR notes Test DBs live in OS temp dirs (`tests/support/database.ts` → `mkdtempSync(tmpdir())`) and are recreated per run, so there is no `apps/web/.playwright/db.sqlite` to handle

## 2. Identity layer (`invite-session`)

- [x] 2.1 Extend `resolveInvite()` in `apps/web/src/lib/invite-session.ts` to return `kind: "individual" | "member" | "group"` (derived: `parentId != null` ⇒ member; `type === "group"` ⇒ group; else individual) AND `parentId` (needed by the sticky rule in `[id].ts` to check the cookie member belongs to THIS group); for groups, also fetch `maxMembers` and `claimedCount` (`COUNT(*) WHERE parent_id = id`) in the same resolution path
- [x] 2.2 Update `GET /api/invite/me` (`apps/web/src/pages/api/invite/me.ts`) to return `{ displayName, kind }`, plus `group: { maxMembers, claimedCount }` for group cookies; keep the uniform bare 404s and `Cache-Control: no-store` exactly as-is
- [ ] 2.3 Update the exact-shape assertion at `apps/web/tests/rsvp-api.spec.ts:121` to `{ displayName: "Guest Two", kind: "individual" }`, and confirm `rsvp-api.spec.ts:117-118` (link rebind for a standalone individual with a foreign cookie) still passes unchanged — it is the regression guard for D4's "all other cases rebind". Add coverage: member cookie ⇒ `kind: "member"` with the member's own name; group cookie ⇒ `kind: "group"` with correct quota state; anonymous/malformed/unknown unchanged

## 3. Slot claim endpoint & sticky cookie

- [x] 3.1 Create `apps/web/src/lib/group-claim.ts` with `claimMemberSlot(group, displayName, now)`: a single atomic parameterized `INSERT INTO invites (id, display_name, created_at, parent_id, type) SELECT … WHERE (SELECT COUNT(*) … parent_id = ?) < (SELECT max_members … id = ?) RETURNING id, display_name` (design D3 — the ONLY quota guard; say so in a comment). Issue it via Drizzle's SQL template (`db.all(sql`…`)`) or `db.$client.execute({ sql, args })` — `db.execute` does not exist on this driver; never interpolate `displayName`; both subqueries bind the same `group.id`. Collision classifier MUST be precise (`/UNIQUE constraint failed: invites\.id/i` or libsql `SQLITE_CONSTRAINT_PRIMARYKEY`/`_UNIQUE` codes); any other constraint error fails fast (claim ⇒ `503`), never retries; each retry re-executes the whole conditional statement with a fresh id. Extract shared helpers instead of duplicating them: move `generateInviteId`/`INVITE_ALPHABET` (currently module-private in `apps/web/src/pages/api/admin/[token]/invites.ts`) into a shared lib (e.g. `apps/web/src/lib/invite-id.ts`) so admin-create and claim cannot drift
- [x] 3.2 Create `apps/web/src/pages/api/invite/claim.ts` (`POST`): resolve cookie identity BEFORE body parsing; missing/malformed/unknown ⇒ bare `404`; standalone individual ⇒ `409 { error: "not_a_group" }`; member ⇒ idempotent `200 { displayName, kind: "member" }`; group ⇒ validate `{ displayName }` (trim, 1–120) ⇒ `400` on failure, then claim ⇒ `201 { displayName, kind: "member" }` with `Set-Cookie` rebinding `ww_invite_id` to the member id using the FULL attribute set from `[id].ts` (`httpOnly: false`, `path: "/"`, `sameSite: "lax"`, `secure` in production, `maxAge` from `INVITE_COOKIE_DAYS`); empty RETURNING ⇒ re-check group existence (gone ⇒ `503`, present ⇒ `409 { error: "group_full" }`); storage failures (incl. `SQLITE_BUSY`) ⇒ `503`, never `group_full`; `no-store` on every response; no metrics writes. Likewise extract the cookie attribute set + `toCookieMaxAgeSeconds` (currently private to `[id].ts`) into `invite-session.ts` (or a shared `invite-cookie.ts`) and have `[id].ts`, `claim.ts`, and any future cookie writer consume it, so 3.4's attribute-parity test guards one source of truth rather than three copies
- [x] 3.3 Update `apps/web/src/pages/[id].ts`: add `type` to both the metrics `UPDATE … RETURNING` and the degraded-path existence `SELECT`. When the link id is a group, resolve the incoming cookie's invite (reuse `resolveInvite` — the handler does not read the cookie today) and, if it maps to a member whose `parentId` equals THIS group's id, re-set the cookie with the SAME member id and a fresh `Max-Age` (sticky identity + sliding expiry); on cookie-resolution error fall back to today's rebind behavior — never a 5xx on a navigation. When the query string has `fresh=1` on a group link, skip the sticky rule and rebind to the group id (release path — no writes, no slot consumed); all other cases rebind exactly as today. Metrics always bump on the link id
- [ ] 3.4 Unit/integration coverage: first claim ⇒ 201 + rebound cookie + member row with correct shape; claim `Set-Cookie` is attribute-for-attribute identical to the link redirect's and readable from `document.cookie`; re-claim idempotent; `not_a_group`; `group_full` at the boundary; invalid bodies ⇒ 400; anonymous ⇒ 404; member revisit keeps the member id with refreshed `Max-Age` while `seen_count` increments; `?fresh=1` rebinds to the group and leaves the member row intact; `?fresh=1` on an individual link is a no-op; **cross-group rebind** (spec scenario "Member of another group follows this link"): cookie = member of group A, `GET /{B}` ⇒ `Set-Cookie` = B and group A's member row is unchanged; **storage failure ⇒ 503, never `group_full`** (spec scenario "Storage failure is not a full group"): force the claim statement to throw and assert `503` with no member row created. NOTE: `photo-publication.spec.ts`'s dependency-injection pattern does NOT apply here — `publishGuestPhoto(id, bytes, deps)` takes its publisher as a parameter, but `claimMemberSlot(group, displayName, now)` imports `db` at module level and has no injection seam, and a Playwright spec cannot close or replace the dev server's connection. The technique that works against `tests/support/dev-server.mjs` is to hold `BEGIN EXCLUSIVE` (or any long write transaction) on the test's OWN connection to the same SQLite file so the server's claim INSERT fails `SQLITE_BUSY`, assert `503` and no member row, then roll back. Adding an injection seam to `claimMemberSlot` is the alternative, but that is a code change, not a test task.

## 4. Mutation guards on RSVP & guest-photos

- [x] 4.1 `apps/web/src/pages/api/rsvp/index.ts` POST: after resolution, `kind === "group"` ⇒ `409 { error: "claim_required" }` (anonymous stays bare 404 before body parsing); GET unchanged (group cookie naturally reads `{ attending: null }`)
- [x] 4.2 `apps/web/src/pages/api/guest-photos/index.ts` POST: same `409 claim_required` guard immediately after resolution and the origin guard, before any multipart handling (order matters: a cross-origin caller holding a group cookie gets `403`, not `409`, so group state is not disclosed to a foreign origin); GET payload unchanged (stays identity-minimal)
- [ ] 4.3 Coverage: group-cookie POSTs rejected with `409` and zero side effects (no `rsvps` row, no photo row/files); two members of one group RSVP independently (`rsvp` rows keyed by each member id; `/api/rsvp/count` counts attending members); two members upload one photo each (distinct rows, per-member `mineId`, duplicate ⇒ `409 already_posted`); group-cookie READS stay valid and unguarded (the two ADDED scenarios): `GET /api/rsvp` ⇒ `200 { attending: null }` and `GET /api/guest-photos` ⇒ `200 { inviteValid: true, mineId: null }` — no code change expected, but assert it so a future over-eager guard is caught

## 5. Client surfaces

- [x] 5.1 RSVP surface: extend the inline invite query in `apps/web/src/pages/index.astro` (it does NOT use `resolveInvite`) with `type`, `parentId`, `maxMembers` and pass `inviteKind`/`groupQuota` props to `apps/web/src/components/RsvpSection.astro`; gate its invitee controls on `kind !== "group"`, rendering the name-prompt claim form while `claimedCount < maxMembers` and the read-only "group is at capacity" state when full. On claim success do a FULL PAGE RELOAD (the confirm controls are server-rendered and absent from group-cookie HTML — only a reload puts RSVP, greeting, and photo surfaces on one consistent member identity). For member visitors render a "Not you? Claim your own spot" **control** targeting `/{groupId}?fresh=1` behind a TWO-STEP confirmation (a `<button data-release-control>` with the URL in `data-release-href` — NOT an anchor, see task 7.4) (confirm dialog or two-stage control — the release is one-way for that browser and a re-claim consumes another slot); handle `409 claim_required`/`group_full` by reloading to re-resolve identity
- [x] 5.2 Photo surface in `apps/web/src/scripts/photo-trail.ts`: check `GET /api/invite/me` (already cookie-gated per invite-session) before revealing the upload CTA to group identities (claim prompt / capacity state) — share ONE claim affordance/flow with the RSVP surface rather than two ad-hoc prompts; **narrow the `response.status === 409` success branch to `already_posted` only by reading the JSON error body** — `committed` MUST never be set for `claim_required` or an unreadable/unrecognized 409 body (those follow the uncertain-outcome refresh path); `claim_required` routes to the claim state and `group_full` to the capacity state (states + entries in `PHOTO_ERRORS`)
- [x] 5.3 Greeting sanity: group cookie shows the group `displayName` server-side pre-claim; post-claim member cookie shows the member name (no greeting code changes expected — verify)

## 6. Admin API

- [x] 6.1 `POST /api/admin/[token]/invites.ts`: accept optional `type` (`"individual"` default | `"group"`) and `maxMembers`; group requires integer 2–50 else `400 invalid_max_members`; individual rejects `maxMembers`; any `parentId`/unknown `type` ⇒ `400`; group 201 response includes `type: "group"` + `maxMembers`, individual 201 gains `type: "individual"`; bare-404 token gating unchanged. Replace the existing admin collision classifier (four substring checks: `"UNIQUE" || "constraint" || "PRIMARYKEY" || "SQLITE_CONSTRAINT"`) with the precise shared helper from 3.1 (CHECK/FK failures must fail fast as `500 invite_create_failed`, never retry)
- [x] 6.2 `GET /api/admin/[token]/invites.ts`: return standalone individuals, groups, AND member rows as top-level entries with `type`/`parentId`; for group rows attach `maxMembers`, `claimedCount`, `attendingCount`, `declinedCount`, `photoCount`, and `members[]` (id, displayName, attending, openedAt/openedCount, hasPhoto — NO `seen_*`: normally zero for members because link metrics bump on the link id, though a member id used directly as a link would bump it) via one query over `invites`/`rsvps`/`guest_photos` with in-JS grouping — the intent is no N+1 / per-member queries, not a single literal SQL statement
- [ ] 6.3 Coverage: create group (201 shape + stored row, individual 201 carries `type: "individual"`); validation failures, asserting the specific `400` code in each case (`invalid_type` for an unknown `type`, `invalid_max_members` for a group with a missing/out-of-range/non-integer `maxMembers` AND for an individual carrying a non-null `maxMembers`, `invalid_parent_id` for any non-null `parentId` — note an explicit `null` for either is accepted, not rejected); the spec's mixed-list scenario returns EXACTLY four top-level entries (individual + group + two member rows) with the group breakdown nested under the group entry; empty group lists with zeroed counts

## 7. Concurrency & E2E verification

- [ ] 7.1 New Playwright spec `apps/web/tests/group-invitations.spec.ts` booting a real server (`tests/support/server.ts`, raw-SQL seeding): concurrent claim storm — 5 parallel claims on `maxMembers=3` ⇒ NEVER more than 3 member rows (exactly 3 when no claim returns `503`) and at most 3 × 201; losers receive `409 group_full` or a retryable `503`
- [ ] 7.2 E2E member isolation: two browser contexts claim on one group link, RSVP differently, upload distinct photos; verify per-cookie reads, admin breakdown, and no cross-member leakage
- [ ] 7.3 E2E capacity UX: visitor N+1 on a full group browses (greeting, photos wall, RSVP count) while claim/RSVP/photo surface the capacity/claim-required states; assert a group-cookie upload attempt leaves the photo CTA in the claim state with an empty status region — NOT the `SYNC_ERROR` "already uploaded" message
- [ ] 7.4 E2E sticky cookie + release (seed the group with an OPEN SLOT before the release step — the D2 capacity gate refuses `?fresh=1` at capacity, so reusing a two-of-two fixture makes the release assertion fail against correct behaviour): claimed member re-opens the group link ⇒ identity persists (RSVP state still shown, cookie value unchanged, `Max-Age` refreshed); the "Not you?" **control** demands confirmation before navigating — it is a `<button data-release-control>` with the target in `data-release-href`, NOT an anchor, so do not assert on an `href` (a static href would let middle-click / context-menu / iOS long-press / drag / copy-link bypass the two-step entirely); the two activations must be separated by the `MIN_ARMED_MS` dwell, and the arm/disarm announcements live in `[data-release-hint]` while `[data-release-hint]` is NOT the `aria-describedby` target (that is the static `#rsvp-release-desc`); a context opening `/{groupId}?fresh=1` gets the group identity, the original member row/RSVP/photo remain intact, and a re-claim from the released browser consumes a NEW slot (visible in admin breakdown); fresh/incognito visitor on the plain link gets the group identity
- [ ] 7.5 Run the full Playwright suite + `pnpm check-types` + `pnpm check`; then `openspec validate group-invitations --strict` (requires the global openspec CLI; if unavailable, verify delta headers match existing requirement names by inspection)

## 8. Documentation

- [x] 8.1 Update `SPEC.md` (rebind rule → sticky-member exception + `?fresh=1` release; `invites` column list + CHECKs; admin POST/GET group contract; the `409` taxonomy split BY ENDPOINT — claim ⇒ `not_a_group`/`group_full`; RSVP ⇒ `claim_required`; photos ⇒ `already_posted`/`claim_required` (`group_full` is never an RSVP or photo outcome)), `README.md` §"Current contracts" and `NOTE.md:25` (both state the unconditional rebind rule — add the sticky exception + claim endpoint), and `ops/README.md` / `ops/MODERATION.md` (group rows; deleting a group with members now fails the self-FK — mechanical backstop to the no-delete rule)

## 9. Review-driven hardening and owner decisions (rounds 1–3)

Work produced by three adversarial review rounds and the owner's decisions on them. Recorded here so the archive reflects what actually shipped; see `handoff.md` for the findings and `design.md`'s risk register for the decisions.

- [x] 9.1 Precise invite-id collision classifier (`lib/invite-id.ts`): anchored `^UNIQUE constraint failed: invites\.id\b` plus `code`/`extendedCode` sets, so a guest cannot poison `displayName` with the collision string and turn every `SQLITE_BUSY` into a phantom collision; `redactInviteError()` keeps guest display names out of container logs
- [x] 9.2 Post-commit reconciliation in `claimMemberSlot` (`findCommittedClaim`, keyed on the exact generated id AND the group), so a claim that commits and then loses its response returns `201` instead of `503` and cannot be retried into a second member row
- [x] 9.3 Sec-Fetch gate on `GET /{id}?fresh=1` (`isExplicitReleaseNavigation` in `pages/[id].ts`): a state-changing GET is exempt from origin checks and `SameSite=Lax` sends the cookie cross-site, so the release is honoured only for `Sec-Fetch-Site: same-origin`/`none` + `Sec-Fetch-Dest: document`
- [x] 9.4 Release control changed from `<a href>` to `<button data-release-control>` + `data-release-href` + `location.assign`, so middle-click / context-menu / iOS long-press / drag / copy-link / no-script taps cannot bypass the two-step; plus a `MIN_ARMED_MS` dwell, a focus-aware self-rescheduling disarm tick, and a separate `aria-live` region for the arm/cancel announcements
- [x] 9.5 Release withheld when the parent group has no open slot — client (`parentGroupFull` in `RsvpSection.astro`, computed from the parent quota `index.astro` now fetches for members) AND server (capacity gate in `pages/[id].ts`, fail-closed on an unreadable quota)
- [x] 9.6 **Owner decision D1 (option A):** `security.checkOrigin: false` in `astro.config.mjs` plus a proxy-aware `isSameOriginRequest()` in `lib/same-origin.ts` on every cookie-authenticated POST (`claim`, `guest-photos`, `rsvp`, `invite/opened`), with a uniform `403 { "error": "cross_origin_post" }`. Fixes a pre-existing production defect: behind `Caddyfile.example` the framework check 403'd guest photo upload and invite-open tracking, while `/api/rsvp` survived only because JSON bodies are exempt
- [x] 9.7 Architecture test `apps/web/tests/origin-guard.spec.ts`: fails if a mutating endpoint omits the guard, if `checkOrigin` is re-enabled, if a cross-origin POST is not refused, or if a proxied same-origin POST is. Mutation-verified — removing the guard from one endpoint and re-enabling `checkOrigin` each fail it
- [x] 9.8 **Owner decision D3:** accepted the unclosable claim duplicate (process death mid-statement) and reworded the ambiguous-outcome copy (`AMBIGUOUS_CLAIM_COPY` in `RsvpSection.astro`) so a guest is steered away from a blind retry after a `503` or a thrown fetch; corrected `ops/MODERATION.md`, which had claimed deleting a member row orphans their records when it in fact fails on the child FK
- [x] 9.9 **Owner decision D2:** filed `openspec/changes/release-via-post/` (POST conversion) and `openspec/changes/member-slot-release/` (designed slot-release procedure) as follow-up changes, with proposals capturing the constraints and the rejected alternatives
- [x] 9.10 Photo trail: fourth `"unknown"` identity state that fails closed for the pill without stranding an eligible individual (404 stays a definitive `"none"`; anything else retries on a 3 s timer rather than waiting for a tab switch), probe started eagerly so it runs in parallel with the first collection read, `409` branches invalidate the cached probe, and the claim affordance no longer offers a flow the server did not render
- [x] 9.11 Shared `readGroupQuota()` in `lib/invite-session.ts` (one query, `claimedCount` always derived), used by both `index.astro` branches and the `[id].ts` capacity gate; `index.astro`'s member quota lookup has a scoped `try/catch` so an auxiliary failure cannot discard a resolved identity
- [x] 9.12 Admin list ordering tiebreaker (`createdAt DESC, id DESC`) so a claim storm's same-millisecond members are deterministic; `members[]` ordering documented in the spec delta and `SPEC.md`
- [x] 9.13 Artifact sync for all of the above: spec deltas (`group-invitations`, `invite-session`, `rsvp-section`), `design.md` Goals/D4/risks, `SPEC.md`, `README.md`, `NOTE.md`, `ops/README.md`, `ops/MODERATION.md`
