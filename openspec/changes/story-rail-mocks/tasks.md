# Tasks: story-rail-mocks

## 1. Rail targeting fix (the reported bug + the locator that masked it)

- [x] 1.1 Add `data-story-rail` to the rail's flex container in `apps/web/src/pages/index.astro` (the `flex gap-2 overflow-x-auto pb-2 no-scrollbar snap-x px-6` div holding the add-story tile, teasers, and demo StoryViewers). In `apps/web/src/scripts/guest-rail.ts`, replace `findRail()`'s heading-text + `parentElement.parentElement` traversal with `document.querySelector("[data-story-rail]")`; delete the old lookup entirely.
- [x] 1.2 Retarget `apps/web/tests/guest-rail.spec.ts`'s `RAIL` locator from `section:has(h2:text-is('Show Our Memories Together'))` (the section — passes whether tiles land in the rail or below it, which is how this bug shipped) to `[data-story-rail]`, and add a direct-descendant assertion: guest tile wrappers are children of the rail container. Also delete `apps/web/tests/rail-helpers.ts` (dead file, zero importers, carries the same stale locator).
- [x] 1.3 Verify: as an invitee with wall stories present, guest tiles render **inside** the rail's horizontal scroll row (snap, gap, 112×200 sizing all apply), not under it. Post 3 photos + a wish → own tile appears in the rail. Story-viewer hand-off (next/prev across tiles) still follows rail order. Public rail unchanged. Updated spec fails against the pre-fix code and passes after.

## 2. One render path + StoryViewer component fixes (mock prop + leak-free removal)

- [x] 2.1 Rework `guest-rail.ts` so initial load and `submissions:posted` share one render function: clear existing `[data-guest]` tiles, append `wall.stories` (cap 30) followed by `mine`'s tile (when it has photos). Drop the `guestTilesRendered` flag (clear-then-render is idempotent). Rename `RAIL_TILE_CAP` → `WALL_TILE_CAP` (it's the wall slice, not a rail cap) and refresh the budget comment (10 demos + ≤3 teasers + 3 mocks + 30 wall + 1 own). Note: dynamically built tiles never animate — `.animate-fade-up` is a scoped style in `index.astro` that doesn't match runtime-created nodes (pre-existing, benign); mocks _will_ animate and their real-tile replacements won't.
- [x] 2.2 Add an explicit `mock?: boolean` prop to `StoryViewer.astro`'s `StoryViewerProps` and emit `data-mock={mock ? "" : undefined}` on the `<story-viewer>` element. The component destructures exactly five props with no `...rest` spread, so without this `data-mock` is silently dropped — eviction (D6) and mock-aware selectors both depend on it.
- [x] 2.3 In `StoryViewer.astro`'s `init()`, store the portaled modal on the element (a `#modal` private field). Extend `disconnectedCallback` to: call `this.closeStory({ animate: false })` (its `!isOpen` early-return makes a never-opened tile a safe no-op), then `this.#modal?.remove()`, then remove the keydown listener. `clearProgress`/`currentSlideEl`/`modal` are `init()`-closure locals — NOT reachable from the class method — and the modal is portaled to `document.body` (so `this.querySelector("[data-modal]")` is null after init); `closeStory` + the stored field are the only correct handles. `closeStory` defaults to a no-op for a tile whose `init()` bailed early — null-tolerance is required.
- [x] 2.4 Verify: invites A and B both post photos; when B posts, A's tile stays in B's rail and B's tile is appended (no reload). A fresh invite posting wish-only removes no guest tiles and adds none (add-story tile still hides). **Reload persistence: post photos → reload → own tile still in the rail; as the sole submitter, reload → rail shows own tile, not an empty guest strip.** Programmatically remove a tile whose modal is open → body scroll restored, no interval errors, no `story-viewer-end` from detached nodes, no orphaned `[data-modal]` in body.

## 3. Mock story tiles (empty-wall placeholders)

- [x] 3.1 In `index.astro`, add the SSR gate (uniform / cookie-blind, D4): render mocks when a `submission_photos` existence check (`LIMIT 1`, try/catch, DB error → fail open) returns zero rows; omit mocks for all visitors (public AND invitee) once any row exists — all mocks disappear the moment the first real story lands. Render three mocks **after the demo tiles** (the tail slots real guest tiles occupy). Each mock MUST be wrapped in its own `snap-center flex-shrink-0 animate-fade-up` div (eviction walks `parentElement` from the `<story-viewer>` element — a mock mapped directly into the flex row would delete the entire rail), with stagger delays continuing the `(storyTeasers.length + storyUsers.length + index) × 60ms` sequence. Pass `mock` to each `StoryViewer` (task 2.2).
- [x] 3.2 Define the mock data inline in `index.astro`'s frontmatter (the `storyUsers` precedent; couple-editable): three named users (suggested: Rara / Bima / Tania), each with avatar + 2–3 stories from the existing `/0–18.webp` pool AND a `timestamp` (e.g. a recent ISO string — an omitted one leaves `[data-timestamp]` visibly empty, a parity break against demos). Keep mock slides off `/4.webp` and `/7.webp` (nicety — avoids transient two-visible-modal flake; the assertions are modal-scoped and can't truly collide).
- [x] 3.3 In `guest-rail.ts`, add `syncMockTiles(data)` removing every `[data-mock]` element's wrapper (via `parentElement`, through the D3 leak-free path) when `wall.stories.length > 0 || (mine?.photos.length ?? 0) > 0` — the `?? 0` is required (bare `mine?.photos.length > 0` is `TS18048` under strict mode). Call it after the initial `loadSubmissions()` and in the `submissions:posted` path (after refetch). Invitees only — the public never runs this (their mocks are governed by the SSR gate alone, D4).
- [x] 3.4 Update `guest-rail.spec.ts`: demo-tile counting becomes `story-viewer:not([data-guest]):not([data-mock])`; give the DOM-order check a real three-way classification (teaser / demo / mock / guest — today every non-`[data-guest]` story-viewer labels as `"demo"`). In the hand-off test, add an explicit `await expect(page.locator("[data-mock]")).toHaveCount(0)` barrier after posting and before clicking the last demo — otherwise the last demo's `story-viewer-end` can hand off to a still-attached mock (mocks are present in every test run via fail-open) instead of the guest tile, and the test would fail on `modalSrc`. `story-viewer.spec.ts` needs no changes (tail placement keeps `.first()`/`.nth(1)` on demos) — run it to prove it.
- [x] 3.5 New Playwright coverage (scratch DB is empty → fail-open means mocks are the default test condition): empty wall → three mocks present, public run makes zero submissions-endpoint requests; post photos via API → mocks evicted without reload; wish-only post → mocks persist; **eviction while a mock's modal is open** (open a mock → route a payload with stories → dispatch `submissions:posted` → assert `body > [data-modal]` count drops, `document.body.style.overflow === ""`, no console errors); hand-off from the last demo lands on the first mock.
- [x] 3.6 Manual verify: fresh DB → public + invitee see 3 mocks; mocks open the modal (progress, gestures, keyboard, hand-off both directions). SSR/client skew (post between render and fetch) → evicted at init. Simulated DB error (garbage `DATABASE_URL`) → mocks render. Reload after first photo post → SSR omits mocks.

## 4. Rotating wish placeholder

- [x] 4.1 In `add-story-flow.ts`: template list (each ≤ 30 UTF-16 code units — verified); a ~4s interval that swaps `[data-wish-input]`'s `placeholder` only while the flow is open and the input is empty. **One pass through the list per open, then settle on the default "Write a wish…".** A `hasTyped` latch (first keystroke) disables rotation for the rest of the page session — clearing the field does not resume it. `openFlow()` clears any existing interval before starting (double-open must not stack timers); `closeFlow()` stops the timer and restores the default placeholder — note it does NOT clear `wishInput.value`, so a reopen can begin non-empty (rotation must not run on a non-empty input, and `hasTyped` correctly stays latched). No interval under `prefers-reduced-motion`.
- [x] 4.2 Verify: placeholder cycles and settles after one pass; first keystroke disables rotation (type → clear → stays settled); reopen after typing does NOT restart rotation; reopen with typed text starts non-empty and stays static; close/reopen leaves no duplicate timers; programmatic second `openFlow()` doesn't stack intervals; reduced-motion → static; marquee's five demo wishes untouched.

## 5. Close-out

- [x] 5.1 Archive `guest-submissions` before archiving this change (the `guest-photos` and `add-story-flow` deltas need their base specs in `openspec/specs/`); reconcile `story-teaser`'s stale "no add-story tile" requirement at that archive.
- [x] 5.2 `pnpm check-types` + production build pass (venue-map-routes 5.12 / rsvp-live-count 5.2 precedent).
- [x] 5.3 Full rail regression across the four states — public / invitee-empty / invitee-with-wall / invitee-posted: tile order, hand-off navigation, add-story tile visibility rules, no console errors, no orphaned modals — plus the full Playwright suite green.
