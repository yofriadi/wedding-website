## 0. Resolved decisions (design Open Questions 1 and 3)

**Both decisions are settled by the owner.** These four tasks are now _confirm and record_, not _decide_ — do not reopen them, and do not substitute a different number. Where a task says "verify", it means check the stated outcome still holds in the rendered page, not choose again.

- [x] 0.1 **Font axis — RESOLVED: Fraunces `standard`** (wght + opsz, 145.3 KB latin). The owner chose optical-size correctness on the display type over the 65 KB saving from `wght`-only: the hero ampersand renders up to ~120 px via `clamp(2.5rem, 11.5vw, 7.5rem)`, which is exactly where `opsz` earns its bytes, and this is a site whose large lettering _is_ the design. Gates 1.1, 1.6 and 2.2. Verify by eye that the ampersand renders as expected after the entrypoint swap **Done:** recorded in `design.md` OQ1. Verified by measurement rather than by eye — the ampersand computes to `Fraunces Variable` italic at 120 px with `font-optical-sizing: auto`, its rendered advance (70.08 px) matches a canvas measure of Fraunces (69.59 px) and not generic `serif` (93.34 px), and `opsz` is measurably driving: advance-per-em is 0.6844 at 16 px vs 0.5799 at 120 px. A human glance at the screenshots is still worth having; see `measurements.md`.
- [x] 0.2 **Budget — RESOLVED: ≤ 200 KB transferred for a latin-only render.** Arithmetic: Fraunces `standard` latin 145.3 KB + Geist latin ≈ 28.6 KB ≈ 174 KB, plus the retained Geist `latin-ext` (16.1 KB, fetched only when a guest name matches its range) ≈ 190 KB. Record this in `design.md` Open Question 1 as resolved; the D6 axis table stays as reference **Done:** recorded in `design.md` OQ1, D6 table left as reference. Measured **179 124 b** across 3 requests — inside the ≤ 200 KB budget, down from the 293 KB baseline.
- [x] 0.3 **Collage `sizes` — RESOLVED: per-slot, not one global cap, keyed on the existing `isCenter` flag.** The owner's rule: the centre image ends the sequence filling the viewport, so it is sized to the viewport; the nine surrounding images are never the focus and are largely clipped by the stage, so they are sized far lower. This fits the code rather than fighting it — `isCenter` is already first-class (`data-is-center` at `ZoomParallax.astro:107`, `zIndex` 50 vs 10 at `:110`, and its own branch in the scrub at `:376-380`), and `centerScaleFactor = max(stageW/innerW, stageH/innerH) × 1.01` (`:364-368`) exists precisely to make that slot cover the stage. Concretely: **centre slot `sizes="min(100vw, 1280px)"`; the nine outer slots `sizes="min(33vw, 390px)"`** (33.333vw is the grid column at `:117`). At 390 CSS px @2x the centre resolves to the 768w candidate and the outer nine to 390w; on a 1440 desktop the centre reaches its 1920 px master, which is correct because it genuinely covers the stage there **Done:** implemented verbatim — `SIZES_CENTER = min(100vw, 1280px)`, `SIZES_OUTER = min(33vw, 390px)`, ternary on `picture.isCenter` in `ZoomParallax.astro`. **One prediction corrected by measurement:** at 390 CSS px @2x the centre resolves to `center-focus-w1280.avif`, not 768w — `min(100vw,1280px)` is 390 CSS px there and DPR 2 makes the demand 780 device px, 12 past the 768w rung. The outer-nine half of the prediction is correct (390w at 390, masters at 1440), as is the centre's 1920w master at 1440. No normative number is violated and nothing was retuned; see `design.md` OQ3 and `measurements.md`.
- [x] 0.4 **Record the trade-off, and why it is perceptual rather than geometric.** Geometrically the outer slots want _more_ resolution, not less: an image scaled 8–9× from a ~130 px slot inside an `overflow-hidden` stage (`:101`) is a magnified crop, so perfect sharpness would need roughly 2300 device px of source. They are underserved anyway because they are fast-moving peripheral content — the config comments call them "Fast (Outer edge)", "Fast (Side edge)", "Fast (Corner)" — and motion masks resolution loss. **Do not "correct" the outer `sizes` upward on geometric grounds**; that reasoning is already accounted for and rejected. Note in `design.md` Open Question 3 that both numbers are starting values to confirm visually during 5.1, and that the centre cap is what bounds 5.9 **Done:** `design.md` OQ3 already carried the perceptual-vs-geometric argument and the "do not correct upward" instruction; extended with the measured confirmation and the 780-device-px correction above.

## 1. Font delivery (design D6)

- [x] 1.1 In `apps/web/src/layouts/Layout.astro`, replace the `@fontsource-variable/fraunces/full.css` and `full-italic.css` imports with the entrypoint chosen in 0.1 (default `standard.css` + `standard-italic.css`); keep both normal and italic — `HeroZoom.astro:58` renders the ampersand italic and `RsvpSection.astro:252` renders `font-serif italic`
- [x] 1.2 Remove the four `@fontsource/geist-sans/{400,500,600,700}.css` imports and drop `@fontsource/geist-sans` from `apps/web/package.json`; keep `@fontsource-variable/geist/wght.css`
- [x] 1.3 Drop the now-dead `"Geist"` entry from `--font-sans` in `apps/web/src/styles/global.css`, and the identically-dead `"Fraunces"` entry from `--font-serif`; sweep the `font-family: var(--font-sans, "Geist", sans-serif)` fallback string at `FamiliesReveal.astro:245` for the same dead name
- [x] 1.4 Verify against the **request list**, not the emitted CSS: stock fontsource entrypoints emit a `unicode-range`-gated `@font-face` for every subset they cover (three for Fraunces `standard`, five for Geist `wght`), so Vietnamese and Cyrillic declarations will still be present. Assert no Vietnamese or Cyrillic file is _requested_ on a latin-only render, **and** that no `.woff` (non-`woff2`) file is requested at all — the `webfont-delivery` "No legacy woff is requested" scenario has no other owning task **Done:** verified against the **request list**, not the CSS. On a latin-only render: 3 font requests, `vietnamese` 0, `cyrillic` 0, legacy `.woff` **0** — and none is even emitted (`dist/client/_astro/` holds 11 `.woff2` and no `.woff`), so the "No legacy woff is requested" scenario cannot fail. Vietnamese and Cyrillic `@font-face` declarations are present as expected, gated by `unicode-range`.
- [x] 1.5 Confirm `font-display: swap` on every emitted `@font-face` in `dist/client/_astro/*.css` — this task is the sole owner of the `webfont-delivery` scenario "Text is legible during font download". Already verified present on both Fraunces entrypoints, so no override is expected; add one only if the confirmation actually finds a face without it. Do **not** add `size-adjust`/`ascent-override` faces — the spec deliberately does not require fallback metric matching **Done:** 11 `@font-face` rules emitted in `dist/client/_astro/index.C59do8XA.css`, **11 carry `font-display: swap`**. No override was needed and none was added; no `size-adjust`/`ascent-override` faces introduced.
- [x] 1.6 Measure: font bytes transferred on a first load at 390 CSS px, empty cache, latin-only render, MUST be within the budget fixed in 0.2 (baseline 293 KB across 3 requests). Record the number in the change directory **Done: 179 124 b transferred** (174.9 KiB / 179.1 KB decimal) across 3 requests, 390 CSS px, empty context, latin-only — against the ≤ 200 KB budget and the 293 KB baseline. Per-file breakdown and reproduction steps in `measurements.md`.

## 2. Resource hints (design D7)

- [x] 2.1 Add `preconnect` for the basemap tile origins (`{a-d}.basemaps.cartocdn.com`, per `venue-map.ts:518,523` `subdomains: "abcd"`). Choose and state the emission mechanism — `Layout.astro:119` calls these "lazily initialized map tiles" and cannot observe whether `VenueMap` mounted, so this is either a `Layout` prop set by the page or an unconditional body-level hint. **Also state how many origins and why**: four unconditional preconnects opening during head parse, for a below-the-fold lazily-initialized map, is real contention on the 1.6 Mbps link this change exists to protect — one origin or a deferred hint may be the right answer **Done, then corrected in review.** MECHANISM: emitted from `pages/index.astro`'s `slot="head"` block, beside the hero preload — **not** from `VenueMap.astro` at body level, which is where this task first put them. A `<link rel="preconnect">` in the body is an HTML conformance error (the Standard permits a body `<link>` only with `itemprop`); engines honour it there only as a quirk, and this repo treats the Standard as normative elsewhere (the duplicate-`w`-descriptor rule in D1/4.3). The original rationale — "Layout cannot observe whether VenueMap mounted" — does not survive inspection: `index.astro:288` renders `<VenueMap />` unconditionally and this page is the map's only consumer, so the page can observe it and a head-level hint ships exactly when the map does. Layout is still not the emitter, for the reason given. Verified: 4 basemap hints in `<head>`, 0 in the body. COUNT: **one `preconnect` (`a.basemaps.cartocdn.com`) plus three `dns-prefetch` (b/c/d)**, not four preconnects — the map is below the fold _and_ lazily initialized behind an IntersectionObserver plus a dynamic `import()`, so four TCP+TLS handshakes at parse time would be four idle sockets for a widget that may never mount; `subdomains: "abcd"` round-robins by tile coordinate, so no one host is guaranteed first, and DNS is the expensive half of a cold cross-origin lookup. Tiles are plain `<img>` fetches, so the hints carry no `crossorigin`. Reasoning recorded in a comment beside them — as an Astro `{/* */}` expression comment, not `<!-- -->`, so it is stripped from the delivered document.
- [x] 2.2 Add `rel="preload" as="font" type="font/woff2" crossorigin` for the latin serif file chosen in 0.1, resolving the URL **through a Vite import** in the Layout frontmatter (e.g. `import latinSerif from "@fontsource-variable/fraunces/files/fraunces-latin-standard-normal.woff2"`, then `href={latinSerif}`). A literal package path 404s because Vite emits the file into `dist/client/_astro/` under a content hash. Keep total font preloads ≤ 2
- [x] 2.3 Add a hero image preload using `imagesrcset`/`imagesizes` that mirror `HeroZoom.astro:9-21`'s `<img>`, naming the AVIF format the `<picture>` will actually select. A bare `href` cannot express a `srcset` selection and would double-fetch a discarded format. `sizes="min(280vw, 1536px)"` resolves to 1092 px at a 390 CSS px viewport, so DPR 2 needs 2184 device px and no candidate reaches it — the engine takes the largest, **1536w**. Target that, not `-390`. (`280vw` is deliberate: the hero starts at `transform: scale(2.8)`.)
- [x] 2.4 **Own the `fetchpriority` cleanup here, so phase 1 is independently shippable.** `fetchpriority="high"` is on two images: the hero at `HeroZoom.astro:17` and the gate background at `WelcomeGate.astro:49`. Confirm the hero's and **remove the gate's**. The `responsive-media` scenario "exactly one image carries `fetchpriority=\"high\"`" passes today only because `public/gate/gate-bg.avif` is absent and `gateBg` is null (the `hasGateBg` alias this task described was folded away in review) — it would break the moment the couple supplies the asset **Done:** the gate's `fetchpriority="high"` is removed (with a comment recording why it looked harmless), the hero's confirmed. The delivered document contains **exactly one `img[fetchpriority="high"]`**, on `HeroZoom`'s `<img>` — now unconditionally, not only because `gateBg` is null. **Counted over `img` elements**: the document also carries `fetchpriority="high"` on the hero's `<link rel="preload">` (see 2.3), which the `responsive-media` LCP requirement now mandates so the hint and its consumer agree and the preload is not queued below the head's font preload. An assertion that counts occurrences of the attribute in the document yields 2 on correct markup and must be scoped to `img[fetchpriority="high"]`; the scenario now says so explicitly.
- [x] 2.5 Add `loading="lazy"` to every below-fold `<img>` **except** the ten collage images, which stay on the tier observer because the tier, not the viewport alone, decides which candidate loads **Done:** 5 below-fold images carry `loading="lazy"` — the three `TimelineScroll` photos and the two `EventTimes` cards. The hero and its blur layer stay `loading="eager"`; the gate background is in the initial viewport so it is not lazy; the ten collage images are exempt and stay on the tier observer. Measured: **none of the five is requested on a no-scroll load** at either viewport, and all five resolve on a scrolled pageview.
- [x] 2.6 Confirm no `Cache-Control`, `immutable`, or Caddy cache directive was introduced anywhere — `public/` MUST still serve `no-cache` + ETag, which the tier probe's warm-cache locality inference depends on **Done:** the diff adds no `Cache-Control`, `immutable`, or Caddy cache directive; `Caddyfile.example`, `docker-compose.caddy.yml` and `Dockerfile` are untouched. Observed headers unchanged — `public/`, `public/generated/`, `_astro/` bundles and fonts all serve `Cache-Control: public, max-age=0` with an `ETag`, and a conditional GET on a generated variant returns **304 with a 0-byte body**, so the probe's warm-cache locality inference keeps its signal (and gains entries). The only `Cache-Control` writes in `src/` are the pre-existing `no-store` on personalized documents and API responses.

## 3. Overlay floor and the reveal invariant (design D5)

- [x] 3.1 Add a no-script escape for `#loading-screen` in `apps/web/src/pages/index.astro` (`:133`, `fixed inset-0 z-50`, opaque `background-color: var(--loading-bg)` at `:485`, dismissed only by JS via `doHideLoadingScreen` at `:394-401`, whose `loader.remove()` is `:400`), mirroring the in-repo idiom: `<noscript><style>#loading-screen{display:none}</style></noscript>` plus the `@media (scripting: none)` twin `WelcomeGate.astro:100-106/137-141` already uses. Suppression is the mechanism, not inversion — the loader must still ship rendered for the scripted path
- [x] 3.2 Verify the scripted path is unaffected — `<noscript>` is ignored and `@media (scripting: none)` does not match when scripting is enabled, so the loader must still dismiss normally **Done:** on the scripted 390 and 1440 legs `#loading-screen` is **absent from the DOM** after load — faded and removed by `doHideLoadingScreen` exactly as before — and the gate still opens on a swipe-up. `<noscript>` content is ignored and `@media (scripting: none)` does not match when scripting is on, so neither escape touches the scripted path.
- [x] 3.3 In `apps/web/src/scripts/scroll-fade.ts`, make **no behavioral change**; add a comment recording the invariant that delivered markup and initial computed style are opacity 1, that nothing in `src/` sets `[data-scroll-fade]` below 1 outside the reduced-motion block at `global.css:44`, and that the fully-entered boundary is `rect.top <= vh - enterThreshold` (`enterThreshold` at `scroll-fade.ts:44`)
- [x] 3.4 Document the boundary precisely wherever the invariant is asserted: an element inside the bottom `enterThreshold` band (160 px, or 240 px for `data-scroll-fade="card"`) is _partially_ entered and legitimately computes below 1 — at `vh` 844 and `rect.top` 800, `enterProgress` is 0.275 and eased opacity ≈ 0.185. Do not write "everything in the viewport stays at 1"; it is false, and only passes today because no participant sits near the initial fold
- [x] 3.5 Preserve unchanged: the two-pass read/write split, the rented `will-change`, the bidirectional enter/exit behavior, and the `prefers-reduced-motion` early return in both `scroll-fade.ts` and `global.css` **Done, and verified as a no-op:** the diff to `scroll-fade.ts` is comment-only (module header plus the `enterThreshold` boundary note), so the two-pass read/write split, the rented `will-change`, the bidirectional enter/exit and the `prefers-reduced-motion` early return are all untouched; `global.css`'s reduced-motion block is likewise unchanged (only the font tokens moved).
- [ ] 3.6 Add a no-script case to `apps/web/tests/scroll-fade.spec.ts` asserting every `[data-scroll-fade]` participant is visible with JavaScript disabled, so a future CSS rule that hides them fails the suite. Its existing post-scroll opacity assertions (`parseFloat(el.style.opacity || "1")`) and reduced-motion branch are unaffected **Deferred — testing pass.** This task writes a Playwright case; per the apply guardrails no tests are written or run during implementation.
- [x] 3.7 Measure with JS disabled: all reveal participants visible, all intended text visible, and **no element covering the viewport** — the last is the new assertion 3.1 exists to satisfy **Done, measured with JavaScript disabled at 390 @2x:** 17 `<img>` in the delivered document, **0 without a `src`, 0 that failed to decode** (all `complete` with `naturalWidth > 0`, including the ten collage placeholders); all **9** `[data-scroll-fade]` participants at computed opacity 1 with **0** inline writes; and **no element covering the viewport** — `elementFromPoint` at the centre, both edges and the top returns the hero `img` inside `#hero-container` on every probe, with `#loading-screen` present in the DOM but `display: none` (suppression, not inversion). Intended text: 45 of 49 text elements visible, 44 of 47 distinct strings — _more_ than the 43/42 visible with scripting on. The four hidden with JS off are the loader's own shimmer phrase, the gate's two chrome strings (meaningless without a gesture handler) and the JS-only upload CTA; the third `visibility: hidden` "Awal mula" copy is authored inline style, identical with and without scripting, and two of its three instances are visible.

## 4. Variant generation (design D1, D2)

- [x] 4.1 Create `apps/web/scripts/generate-media-variants.mjs` using the existing `sharp@^0.35.4`; emit AVIF + WebP at widths 390/768/1280 named `<base>-w<width>.<ext>` into `apps/web/public/generated/`, plus a ~32 px blurred `<base>-lqip.<ext>` in both formats. **Drive it from the explicit list of base names the markup references** — the same list 4.8 builds — not from a directory scan. Do not scan subdirectories: `public/map/` is the only raster one and its sole consumer (`venue-map.ts:123` `bannerHtml`) is deferred to 7.4, so scanning it would emit 16 files nothing consumes
- [x] 4.2 Bound the input set explicitly: skip non-raster files (`lamaran.mp4`, the 3.1 MB `.mp3`, `favicon.svg`), anything already under `generated/`, the `wedding_photo_blur` pair, and **the whole `wedding_photo*` hero family by name**. Do _not_ rely on a `-w<digits>` suffix test — the hero derivatives are `-390`/`-640`/`-1024`/`-1536`, which that pattern does not match, so the generator would emit the variants-of-variants (`wedding_photo-390-w390.avif`) the rule exists to prevent
- [x] 4.3 **Do not upscale; skip the width the master already fills; always offer the master as the top candidate.** Emit a width only when it is at or below the master's intrinsic width — `square-mid-left` is 473 px, `event-resepsi` 584, `awal-perkenalan` 591, `portrait-bottom-left` and `event-akad` 768 — **and skip any configured width that equals the intrinsic width**, because the master fills that slot: emitting both declares the same `w` descriptor twice, which the HTML Standard forbids within one element's candidate list, and transcodes a 768 px master into a 768 px file nobody needs. Three masters sit exactly on a ladder width (`portrait-bottom-left` 768, `event-akad` 768, `keluarga-yofri` 1280). Then append the master itself at its intrinsic width, following the hero idiom where `/wedding_photo.avif 768w` sits between the `-640` and `-1024` derivatives. Without the master-as-top-candidate rule those three narrow sets contain a single 390 px file, which violates the two-resolution requirement, makes 5.6's multi-candidate `srcset` impossible for `EventTimes`, and _regresses_ `event-resepsi` (it ships its 584 px master today into a full-bleed `w-full` card). Derive markup's candidate list from the same two rules so no `srcset` names a skipped file or a duplicate width, and check the result against 5.9's ceiling
- [x] 4.4 Make the script idempotent — skip outputs newer than their master — and have it **fail when any LQIP exceeds 4 KB**, so the aggregate placeholder cost of a tier cannot grow silently. Report what it generated and what it skipped
- [x] 4.5 Add `apps/web/public/generated/` to `.gitignore`; verify with `git status` that a run against real overlaid media stages nothing
- [x] 4.6 Add **`public/generated/**`** to `turbo.json`'s `build.outputs` — **package-relative**, which is why the existing `["dist/**", ".astro/**"]` mean `apps/web/dist`; a root-relative `apps/web/public/generated/**` matches nothing and caches nothing, silently. Alternatively make generation its own task that `build` `dependsOn`. When verifying against a forced cache hit, inspect the **working tree** `apps/web/public/generated/`, not `dist/` — Astro copies `public/` into `dist/client/`, which the existing `dist/**` entry already caches, so the deployed artifact was never at risk. What this glob restores is the working-tree copy that `astro dev` serves and that 4.4's mtime comparison reads **Done, with a negative control.** `outputs` gains the package-relative `public/generated/**`. Primed, then `rm -rf apps/web/public/generated`, then a `FULL TURBO` cache hit: **with** the entry the working tree is restored (84 files); **without** it the same cache hit restores `dist/client/generated` (84, already covered by `dist/**`) but leaves the working tree at **0 files** — exactly the `astro dev` and mtime-comparison breakage D1 predicts. Confirms both that the glob is load-bearing and that it resolves package-relative.
- [x] 4.7 Wire generation as `prebuild` in `apps/web/package.json`, add a `media:variants` script, and document **that one command** (not a `postinstall` hook — `astro dev` runs no `prebuild`) in `README.md` next to `tools/restore-private-media.sh`
- [x] 4.8 Guard the guest-facing 404 case. There is no build-time HTML to scan under `output: "server"`, so use one or both of: the render-time `existsSync` idiom at `WelcomeGate.astro:19-28` (`gateBgAssetExists`, called at `:30`, checking `../../public${src}` and `../../client${src}`), and a completeness check inside the generator against the explicit base-name list that also drives 4.1 **Done, and the first draft of this did not actually guard the case.** Review found that `resolveMasters()` only caught a listed base with _no_ master — it could not detect the inverse and far more likely error, since `BASES` is hand-duplicated from three components: add a slot to `ZoomParallax.astro` and forget `BASES`, and nothing fails, because `variantSrcset()` emits `/generated/<base>-w390.avif` from the naming convention alone. The task was marked done against a guard that did not exist. Now implemented as `guardMarkupCoverage()`, which reads base names back out of the four referencing sources and fails the build when one has a master in `public/` but is absent from `BASES`; a reference whose master is absent does not fail, so the source-text heuristics cannot produce a spurious build error. `resolveMasters()` was strengthened in the same pass to require **both** formats (a single-format master leaves the other `<source>` naming files that do not exist — worst on the `<img>` fallback path, reached precisely by engines that cannot do AVIF) and to reject a base whose AVIF and WebP masters disagree on intrinsic width (one ladder is emitted for both, so a divergence would ship a file named `-w768` holding fewer pixels than the `768w` descriptor it is served under). All four guards were negative-tested: each fails the build with a message naming the base, and a clean run still produces 84 files. The render-time `existsSync` idiom was **not** extended to the 15 generated bases — they are unconditional, so a build-time failure is the right place; it survives only in `WelcomeGate` and `VenueMap`, whose assets are optional by design.
- [x] 4.9 Leave the existing `wedding_photo-{390,640,1024,1536}` files in place and reference them directly rather than regenerating them
- [x] 4.10 Verify CI (placeholder masters) and a local run after `restore-private-media.sh` (real masters) both produce a complete variant set **Done, both legs.** CI: the 30 placeholder masters extracted from `git show HEAD:` produce **84 files**, largest variant 25.2 KB, largest LQIP 0.46 KB. Local (real media overlaid): **84 files**, largest variant 179.6 KB (`center-focus-w1280.webp`), largest LQIP 0.38 KB. Both legs now also write a 85th file, `generated/manifest.json` — the build-configuration fingerprint that makes a `SETTINGS` edit invalidate the outputs instead of silently no-oping; it is counted separately because it is not a variant. `diff` of the two directory listings is **empty** — placeholder dimensions match the real masters for all 15 bases, so the no-upscale ladder is identical. A second run against unchanged masters writes 0 files and reports `nothing to do`.

## 5. Responsive markup and candidate-swap promotion (design D3, D4)

Design phase 4 splits this group into **four** reviewable units. Land **5a = 5.1–5.4 _plus_ 5.7** as one unit — not 5.1–5.4 alone: the instant 5.1 ships a live LQIP `src`, `media-tiering.spec.ts:43`'s probe reads 10 on every tier and its `:154` `toBe(0)` assertion fails, so 5a without 5.7 leaves CI red and contradicts the Migration Plan's "independently shippable". Then **5b = 5.5**, **5c = 5.6**, **5d = 5.8–5.9**. Keeping the promoter convergence in the same unit as its test update is also what stops it being reviewed alongside unrelated markup churn

### 5a. Collage and both promoters (one unit)

- [x] 5.1 **`ZoomParallax.astro`** — one `<img>` template at `:130` inside `pictures.map(...)` at `:104`, rendering ten. Give each intrinsic `width`/`height` (from the `iw`/`ih` config at `:14-96`) and a **per-slot `sizes` keyed on the existing `picture.isCenter` flag**, per the resolved decision in 0.3: centre slot `min(100vw, 1280px)`, the nine outer slots `min(33vw, 390px)`. Branch on `isCenter` in the template — the flag is already threaded to markup as `data-is-center` at `:107`, so this is a ternary on a value already in scope, not new plumbing. Do **not** use a single global `sizes`, and do not derive one from max presentation scale × slot width: that was the earlier framing, superseded because the outer slots are underserved deliberately (see 0.4). Verify the resolved candidates by eye at both 390 and 1440 before moving on. Then ship the LQIP as a live AVIF `<source>` + WebP `<img src>` pair mirroring the existing `<source data-srcset>` (`:129`) / `<img data-src>` (`:131`) structure, keeping the full-resolution set in `data-srcset`/`data-src`
- [x] 5.2 **Remove both presence-keyed guards in `promoteImage`** (`ZoomParallax.astro:224-235`). (a) The `<img>` guard at `:234`, `!img.getAttribute("src")`, is permanently false once a live LQIP `src` ships, so the placeholder would never upgrade. (b) **The `<source>` guard at `:229`, `source[data-srcset]:not([srcset])`, has the identical defect** — after 5.1 the AVIF `<source>` carries a live `srcset`, so `:not([srcset])` never matches, the `<source>` is never promoted, and because `<picture>` resolves to the first matching `<source>` (the AVIF one, and the ordering below is preserved) **every AVIF-capable engine keeps the 32 px placeholder forever**. That failure is invisible to 5.7's probe and to 6.2. Change the selector to `source[data-srcset]` — the `:not(…)` clause is redundant once promotion is gated by the marker. Then: replace `COLLAGE_IMAGES` (**`:185`**, outside the `promoteImage` range) `#zoom-parallax-container img[data-src]:not([src])` with **`#zoom-parallax-container img[data-src]:not([data-promoted])`**, overwrite both `src` and `srcset`, and set `data-promoted` afterwards as the **sole** idempotency mechanism. Preserve the `<source>`-before-`<img>` ordering
- [x] 5.3 **Converge the inline promoter** in `apps/web/src/pages/index.astro:179-198` on the same attribute names, both selector changes (its `<source>` guard is at `:190`), the marker and the overwrite. The two copies **differ today** (`index.astro:196` writes `src` unconditionally; `promoteImage` guards on `!img.getAttribute("src")`), so the task is to make them equivalent, not to keep them so
- [x] 5.4 Delete or reword every comment in `ZoomParallax.astro` that 5.1 falsifies — five, not two: the markup rationale at `:120-127` ("no live src/srcset ships in the markup, so no collage byte moves until the tier says so"), the `<noscript>` rationale at `:146-153` ("four screens of empty frames" — the collapse is still wanted, but now to avoid four screens of _blur_), "an unpromoted `<img>` is an empty frame" at `:322-324`, the CSS rationale at `:510-513`, and the `.zoom-image:not([src]) { visibility: hidden }` rule itself at `:514-516`, which can never match afterwards. Leaving a false comment describing the tier contract this change inverts undercuts D5's own method. **Done — and then taken further in review.** All five were handled, but the rewritten markup rationale still sat _inside_ `pictures.map()` as an HTML comment, so it was emitted once per slot: 1 164 B × 10 = **11.6 KB** on every pageview, against 768 B × 10 for the version it replaced. Astro strips `{/* */}` expression comments from output, so all seven rationale comments this change added across `ZoomParallax`, `VenueMap`, `index.astro` and `Layout.astro` were converted. The delivered document fell from 151 557 to **137 283 bytes (−14 274, −9.4 %; comment bytes fell 14 295, the 21 B difference being the `fetchpriority="high"` the hero preload gained in the same interval)** — the seven added comments plus `<!-- Inner Image Container -->`, a pre-existing comment inside the same `.map()` that also costs ×10 and was converted in the third review pass. Every invariant was re-checked afterwards rather than assumed: 17 images, 0 without `src`, **one `img[fetchpriority="high"]`** plus the matching one on the hero's preload `<link>`, 44 `srcset`s with 0 duplicate `w` descriptors, 4 tile hints in `<head>` and 0 in the body, 5 `<noscript>` blocks, 0 malformed `/generated/` URLs. On a change whose subject is bytes on a throttled link, 14.3 KB of duplicated rationale is not free — at 1.6 Mbps it is ~71 ms of transfer

### 5b. Timeline

- [x] 5.5 **`TimelineScroll.astro`.** `keluarga-acik` (`:150`, `width="2048"`, 463 KB) and `keluarga-yofri` (`:117`, `width="1280"`) gain candidate sets; `awal-perkenalan` (`:90`) is **already** annotated `width="591" height="1280"` matching its `aspect-[591/1280]` container, so it needs only a candidate set — and at 591 px intrinsic it takes the master-as-top-candidate path from 4.3. This is the change's largest single byte win

### 5c. Events and gate

- [x] 5.6 **`EventTimes.astro:58`** — one template inside `EVENTS.map` at `:47`, rendering twice (`event-akad` 768 px, `event-resepsi` 584 px), already `loading="lazy"` with `width`/`height` — gains a multi-candidate `srcset`; note `event-resepsi` at 584 px yields only `-w390` plus the master, so 4.3's rule is what makes this task satisfiable at all. **`WelcomeGate.astro:45`**: inside the existing `hasGateBg` branch, add `width`/`height` (true intrinsic dimensions; the layout-reservation rationale is vacuous for an `alt=""` absolutely-positioned full-bleed image, but the repo already applies this convention at `HeroZoom.astro:9` and `:27`) and a candidate set. Its `fetchpriority="high"` at `:49` is removed by **2.4**, not here. This element is unreachable in any build this repo can produce — there is no `public/gate/`, and `restore-private-media.sh:22` globs only top-level `public/*` with `cp -v`, which cannot create a subdirectory — so record the candidate set as conditional on the master existing rather than claiming a verified result. Confirm `HeroZoom.astro:9` needs nothing

### 5d. Tests and measurement

- [ ] 5.7 **Update `apps/web/tests/media-tiering.spec.ts` — this is the test that breaks, and it must land in the same unit as 5.1–5.4.** Repoint the promotion probe in `containerInfo()` (`:35-47`) at `:43` from `#zoom-parallax-container img[src]` to **`#zoom-parallax-container img[data-promoted]`**, keeping the container scope, matching D4's marker. Then **add to**, rather than rewrite, the `lite` assertion at `:154`: `expect(far.promoted).toBe(0)` stays true under the new probe and is still the only thing separating lite-before-approach from the four `toBe(10)` sites, so keep it and _add_ the byte-free half — ten live placeholders present, zero non-`-lqip` collage candidates transferred — using the existing `trackRequests()` at `:50-56`. **The site list in this task, in `proposal.md` and in `design.md` was incomplete, and the two missing sites are the important ones.** Beyond the four `toBe(10)` sites named (`:192`, `:221`, `:514`, `:540`) there is `:673`, and — unlike those, which merely become trivially true — `:169` and `:650` are `expect.poll(...).toBe(10)` assertions that follow a scroll-to-approach and are the file's only **behavioral** evidence for the `media-tiering` scenario "Lite guest scrolls to the collage". Both are satisfied before the scroll happens, so they pass vacuously and no reporter distinguishes them from a real pass. Repointing the probe is not enough; those two need a before/after pair to keep discriminating. **Also add the no-op assertion that owns the `media-tiering` scenario "Placeholder is not re-fetched after promotion"**: promote once, re-run the promoter, assert `trackRequests()` records no further collage requests. Check `:192`, `:221`, `:514` and `:540`, which assert `toBe(10)`: they still pass but become trivially true under the new probe, so confirm each still discriminates what it was written to prove **Deferred — testing pass.** This task edits `tests/media-tiering.spec.ts`, which is the test this change inverts; per the apply guardrails no tests are written or run during implementation. **Note for that pass:** `:43`'s probe now reads 10 on every tier, so the `:154` `expect(far.promoted).toBe(0)` lite-before-approach assertion **fails** until it is repointed at `img[data-promoted]`.

**This deferral contradicts an instruction in this file, and the contradiction is recorded rather than resolved.** Section 5 above says to land 5a = 5.1–5.4 **plus 5.7** as one unit, precisely so CI is not left red; the operator's instruction for this pass was to defer every task that writes or runs tests. The operator's instruction wins, so 5.1–5.4 landed without 5.7 and the suite is knowingly broken. Two facts make that worse than the section-5 wording implies and must be carried into the testing pass: `tests/media-tiering.spec.ts` is **untracked** (`git ls-files` returns nothing), and `.github/workflows/` contains only `docker-image.yml` — there is **no CI test job at all**. So "not CI-green" understates it: nothing observes the failure, and the file could be lost entirely without the repo noticing. Committing the test and adding a Playwright job are prerequisites for the warning in section 5 to have any enforcement mechanism.

- [x] 5.8 Expect **no** edits in `event-times.spec.ts` — its only assertions on the affected images are class-based (`card.locator("picture img")` with `toHaveClass(/times-img|object-cover|absolute|inset-0/)`), and 5.6 changes attributes, not classes. Likewise none in `zoom-reveal-once.spec.ts` (its `.zoom-image` selectors at `:36`/`:122` still match, and its reduced-motion assertion reads `getComputedStyle(...).opacity` at `:135`, not the `visibility` rule 5.4 removes), `families-reveal*.spec.ts`, `photo-trail*.spec.ts`, `photo-pipeline.spec.ts` or `trail-animation.spec.ts`. Verify rather than assume, and update this entry if that turns out wrong **Done, and the expectation held exactly — no entry update needed.** The sweep for `data-tier|data-src|srcset|zoom-image|preload|net-tier|data-promoted` lands in only `helpers.ts` (`:86/:94/:102`), `media-tiering.spec.ts` and `zoom-reveal-once.spec.ts` (`:36/:122`). `event-times.spec.ts` needs no edit: its image assertions are `toBeAttached()` plus `toHaveClass(/times-img|object-cover|absolute|inset-0/)` and 5.6 changed attributes, not classes. `zoom-reveal-once.spec.ts` needs none: `.zoom-image` and `[data-is-center="true"] .zoom-image` still match, its geometry assertions read `getBoundingClientRect()`, and its reduced-motion case reads `getComputedStyle(...).opacity` at `:135`, not the `visibility` rule 5.4 removed (the only `visibility:hidden` at `:96` is a probe element the test creates itself). `families-reveal*.spec.ts`, `photo-trail*.spec.ts`, `photo-pipeline.spec.ts` and `trail-animation.spec.ts` contain no `<img>` or `src` assertions at all.
- [x] 5.9 Measure: a 390 CSS px @2x load MUST NOT request `keluarga-acik` at 2048 px (baseline 463 KB). The ceiling MUST is scoped to what this change controls — **no generated candidate exceeds 200 KB on the measured leg** — because the binding asset is `wedding_photo-1536.avif` at ≈182 KB, which the hero selects at this viewport and which 4.9 and 5.6 leave untouched. Record the hero as the binding floor rather than asking an implementer to amend a normative number. With 0.3 resolved, at 390 CSS px @2x the centre collage slot selects its 768w candidate and the outer nine select 390w, so nothing generated competes with the hero on this leg. State the measurement condition: under Good 3G the tier resolves to `lite` (1.6 Mbps ≈ 200 KB/s, below the 220 KB/s floor), so the collage is not promoted at load — measure both at load and across a scrolled pageview. Separately note, but do not gate on, the desktop-retina case: at 1440 @2x the outer `min(33vw, 390px)` resolves to 780 device px, which for a 1024 px master such as `square-top-right` (≈317 KB AVIF) selects the master rather than a generated candidate. That is a fast-connection `full`-tier path and is accepted **Done, measured at 390 CSS px @2x, at rest and across a full scrolled pageview.** The MUST holds: **`keluarga-acik` was never requested at 2048 px** — the scrolled pageview fetches `keluarga-acik-w768.avif` at **39.4 KB** against the 462.9 KB master, a 12× reduction and the change's largest single byte win. **No generated candidate exceeds 200 KB on this leg**: the largest generated file requested is `center-focus-w1280.avif` at 108.5 KB, and the largest generated file on disk is 179.6 KB — **`center-focus-w1280.webp`**, a WebP derived from the 1920 px `center-focus` master, leaving a 20.4 KB (10.2 %) margin on a ceiling whose prose never mentions that format. An earlier version of this note attributed the 179.6 KB to `square-top-right-w768.avif`, which is 95.4 KB; the generator reported a size with no name, and now reports `largest generated variant: <name> <size> (ceiling 200 KB, <n> over)` measured over every variant on disk. Note the ceiling is a **warning**, not a build failure: it bounds a property of the couple's photos, and failing a deployment over one busy real photo would be worse than shipping it. The hero's `wedding_photo-1536.avif` at 182.4 KB remains the binding floor, untouched, exactly as predicted. Measurement condition stated: under Good 3G the tier resolves to **`lite`** (1.6 Mbps ≈ 200 KB/s, below the 220 KB/s floor) and 0 collage images are promoted at load, so both legs were measured. The desktop-retina case is confirmed and measured — **and this note originally mis-attributed it, which review caught.** At 1440 @2x the outer `min(33vw, 390px)` resolves to 780 device px and selects the **master** (`square-top-right.avif`, 318 KB measured) rather than a generated candidate, because 768w falls 12 px short. An earlier version of this note called that "a fast-connection `full`-tier path, accepted", which is false: candidate selection is a function of `sizes` and DPR alone, and the tier decides only _whether_ promotion happens, never _which_ candidate the promoted `srcset` resolves to. A `lite` guest at 1440 @2x who scrolls within the promotion margin pays the same nine masters — **727.8 KB measured on the slow link the tier exists to protect**, not the ≈2.9 MB this note first claimed by multiplying `square-top-right`'s 317.6 KB by nine. Only 7 of the 9 can be affected by the cap at all: `square-mid-left` (473 px) and `portrait-bottom-left` (768 px) have no generated candidate above 390w. `media-tiering`'s "Lite tier pays per approach" scenario permits it, since that scenario governs _no_ candidate transferring before approach rather than the size of what transfers on approach, but the exposure was never sized. Lowering the outer cap to `383px` would land the 768w rung, saving 669.2 − 286.8 = **382.4 KB across the 7 slots that have one** (not ≈1.25 MB across nine), for a 1.8 % further underserve of a slot that is already underserved by design — which is why follow-up §4 is low-priority rather than urgent. **Not applied**: OQ3 reserves both numbers as "starting values to confirm visually during task 5.1", and a byte measurement is not a visual confirmation. Filed as follow-up 4 with the arithmetic, the tier-independence, and the fact that it helps only at DPR 2.

## 6. Verification against the Good-3G baseline

- [ ] 6.1 Commit the throttled harness (Playwright + CDP `Network.emulateNetworkConditions`, 1.6 Mbps / 750 Kbps / 150 ms) so the numbers are reproducible rather than anecdotal, and give it **both** viewport legs: 390×844 @2x and 1440×900 @2x. Every measurement so far is pinned to 390, and the wide leg is the one that would expose a wrong collage `sizes` from 0.3 **Deferred — testing pass.** Per the apply guardrails no tests are written or run during implementation; the whole of section 6 belongs to that pass.
- [ ] 6.2 Assert at rest and at every scroll position, on both legs: zero `<img>` without a fetchable source; zero `[data-scroll-fade]` elements at opacity < 0.05 while **fully entered**; and — owning the `responsive-media` "Wide viewport" scenario — that the 1440 @2x leg selects a wider candidate than the 390 @2x leg from the same `srcset`. **Assert it against a named subject, not against every image:** `keluarga-acik` discriminates (measured `-w768.avif` at 390 @2x, `-w1280.avif` at 1440 @2x), while `event-akad` (768 px master) and `event-resepsi` (584 px master) select their master at _both_ viewports because 780 device px already exceeds it — a per-image universal would fail on correct behavior **Deferred — testing pass.** Per the apply guardrails no tests are written or run during implementation; the whole of section 6 belongs to that pass.
- [ ] 6.3 Assert with JavaScript disabled: all images have a `src`, all intended text is visible, and no overlay — including `#loading-screen` — covers the viewport **Deferred — testing pass.** Per the apply guardrails no tests are written or run during implementation; the whole of section 6 belongs to that pass.
- [ ] 6.4 Assert the tier still resolves to `lite` under Good 3G, and that lite now shows ten blurred placeholders rather than ten empty frames **Deferred — testing pass.** Per the apply guardrails no tests are written or run during implementation; the whole of section 6 belongs to that pass.
- [ ] 6.5 Record DCL and total transferred; target DCL ≤ 2500 ms (baseline 4501 ms). **Also record CLS across a full scrolled pageview** — the `responsive-media` "No cumulative layout shift from media" scenario has no other owner. Confirm the probe's warm-cache locality inference still fires on a second load: `public/` must still produce 304 revalidations, not zero-transfer `immutable` hits **Deferred — testing pass.** Per the apply guardrails no tests are written or run during implementation; the whole of section 6 belongs to that pass.
- [ ] 6.6 Assert the request list on a no-scroll load contains no `loading="lazy"` image, owning the `responsive-media` "Off-screen images are not requested at load" scenario that 2.5 otherwise leaves unverified **Deferred — testing pass.** Per the apply guardrails no tests are written or run during implementation; the whole of section 6 belongs to that pass.
- [ ] 6.7 Re-run the full suite and `openspec validate resilient-media-delivery`. The command is **`pnpm --filter web exec playwright test`** (documented at `README.md:75`) — there is no `test` script in `apps/web/package.json` (its scripts are `astro`, `build`, `check-types`, `dev`, `dev:bare`, `dev:stop`, `preview`, `test:tier-harness`), so `pnpm --filter web test` fails outright. This is the archive-gate task; verify the command runs before relying on it **Deferred — testing pass.** Per the apply guardrails no tests are written or run during implementation; the whole of section 6 belongs to that pass.
- [ ] 6.8 Own the two remaining `progressive-enhancement` scenarios in the throttled harness. **"Late script does not blank the page"**: delay the reveal script past first paint and assert that no _fully entered_ participant (`rect.top <= vh - enterThreshold`, the boundary D5 corrects to) drops below the opacity delivered markup gave it — this is the precise numeric invariant D5 spends two paragraphs establishing and nothing else tests it. **"Script failure leaves content readable"**: block or throw in the script and assert the same for in-viewport participants, accepting that measured off-screen ones sit at their pre-reveal opacity by design **Deferred — testing pass.** Per the apply guardrails no tests are written or run during implementation; the whole of section 6 belongs to that pass.

## 7. Follow-ups and records

- [x] 7.1 **Resolved during review:** `adaptive-media-tiering` archived 2026-09-22 with all 63 tasks closed, so `openspec/specs/media-tiering/spec.md` exists (**8** requirements) and the MODIFIED delta has a real merge target — its heading matches the base verbatim, all five base scenarios are preserved, and two are added intentionally. Re-diff the delta against the base immediately before applying and re-run `openspec validate` on both; one unintended narrowing was already found and fixed (the base scenario reads "a live `src`/`srcset`") **Done, re-diffed immediately before applying.** The delta heading matches `openspec/specs/media-tiering/spec.md` verbatim; the base has **8** requirements; all **5** base scenarios survive in their original relative order and exactly **2** are added ("No tier produces an empty frame", "Placeholder is not re-fetched after promotion"). 7 of the 8 base prose clauses survive verbatim; the eighth, "Promotion SHALL be idempotent", is deliberately strengthened to name `data-promoted` as the sole mechanism. The narrowing the design flagged is corrected — "Full tier fetches at parse time" reads "a live full-resolution `src` **and** `srcset`", keeping `src`. `openspec validate resilient-media-delivery --strict` passes. **The clause-level work is sound but this note's premise is not, and review caught it:** `openspec/specs/media-tiering/spec.md` is **untracked**, as are `families-section/` and `story-teaser/`. The archive commit `5b3088d` creates nothing under `openspec/specs/`, so the base spec on disk is hand-written and uncommitted; `validate` passes only because that file happens to be in this working tree. On a fresh clone or in CI the MODIFIED delta has **no merge target** and archiving would fail or silently drop the amendment — the exact hazard design constraint 3 declares closed. Committing those base specs is a prerequisite for archiving and is deliberately not done here: they are not this change's output, and two are being edited by other in-flight work. Recorded in `design.md` constraint 3.
- [x] 7.2 **Confirmed, not conditional:** `adaptive-media-tiering` task 7.2 was completed (`archive/2026-09-22-adaptive-media-tiering/tasks.md:39`) and produced `apps/web/tests/media-tiering.spec.ts`, which asserts "lite promotes zero images at load" — the exact contract this change inverts. Task 5.7 owns the update; this entry exists so the dependency is not rediscovered mid-implementation **Done, confirmed.** `archive/2026-09-22-adaptive-media-tiering/tasks.md` task 7.2 is `[x]` and its own note records "Lite asserts zero promoted images at load" — the exact contract this change inverts — produced by `tests/media-tiering.spec.ts`. The dependency is recorded here and cross-referenced from 5.7's deferral note so it is not rediscovered mid-pass.
- [x] 7.3 File the deferred caching change: `public/` revalidates ~23 assets per repeat visit (the figure `Layout.astro`'s own LOAD-BEARING comment uses), and moving to `immutable` requires redesigning the probe's locality inference because `immutable` hits and blocked resource timing are indistinguishable in the entries it reads **Done — filed as follow-up 1 in `follow-ups.md`.** Records the ~23-asset figure (now growing by up to 84 generated variants), the precise probe branch that breaks (`readBurst()`'s `if (!body)` locality counter, `MIN_LOCAL_HITS = 5`), why it fails silently and whom it lands on (every returning Safari/Firefox guest), what a redesign must do instead, and the wrinkle that `public/generated/**` names are not content-hashed.
- [x] 7.4 File a follow-up for `venue-map.ts:123` `bannerHtml()`, which emits a photographic `<img>` into the Leaflet popup with no `width`/`height`, no `srcset` and a single-candidate `<source>` for `/map/*.webp` (all three masters present). It is out of scope here because the `responsive-media` requirements are scoped to server-rendered markup, not because it is exempt — and if it lands, `public/map/` becomes a real input and 4.1's no-subdirectory rule must be revisited **Done — filed as follow-up 2 in `follow-ups.md`.** Names `venue-map.ts:123` `bannerHtml()`, the three missing pieces (`width`/`height`, `srcset`, a single-candidate `<source>`), the three masters present in `public/map/`, why the popup banner is the one place intrinsic dimensions genuinely reserve layout, why it is scoped out rather than exempt, and exactly what must be revisited (`BASES`, the `^[a-z0-9_-]+$` test in `guardInputSet()` — which subsumed and replaced the `base.includes("/")` call an earlier draft cited — and output naming).
- [x] 7.5 **Record and file, do not retune here:** whether the 220 KB/s tier floor is still correctly tuned now that a lite tier costs ~40 KB more and renders something instead of empty frames. Tier detection is an explicit non-goal of this change **Done — recorded, filed, not retuned.** Follow-up 3 in `follow-ups.md`. `MIN_FAST_BYTES_PER_MS = 220` is untouched (tier detection is a non-goal). Finding recorded: a lite tier under Good 3G now shows ten blurred placeholders rather than ten empty frames, and they cost **3.27 KB** measured rather than the ~40 KB budgeted, because the LQIPs are 32 px blurred and the 4 KB cap was never close to binding. Both shifts are argued in opposite directions, which is why the follow-up asks for a re-measurement against user-visible outcome rather than bytes, and flags `PROBE_FLOOR_BYTES` / `LOAD_FALLBACK_BYTES` for the same pass.
