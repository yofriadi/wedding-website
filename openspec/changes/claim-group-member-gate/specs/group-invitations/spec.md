# group-invitations Specification (delta)

## REMOVED Requirements

### Requirement: Explicit fresh-identity release for shared browsers

The "Not you? Claim your own spot" release control and `?fresh=1` navigation are REMOVED. Group invitations permanently bind claimed member cookies to that browser profile without a guest-side release path.
