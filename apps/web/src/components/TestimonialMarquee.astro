---
/**
 * TestimonialMarquee Component
 * Extracted from: https://21st.dev/community/components/jatin-yadav05/design-testimonial/default
 * 
 * Displays a scrolling marquee of text in the background.
 */

const wishes = [
  "Wishing you a lifetime of love and happiness",
  "May your journey together be filled with joy",
  "Congratulations on your wedding day",
  "Here's to a long and happy marriage",
  "Best wishes for a fun-filled future together",
];

// Generate enough items to fill the width
const repeatedWishes = Array(4).fill(wishes).flat();
---

<div class="marquee-root">
  <!-- Row 1 -->
  <div class="marquee-track track-1">
    <div class="marquee-content">
      {repeatedWishes.map((text) => (
        <span class="marquee-item">{text}</span>
      ))}
    </div>
    <div class="marquee-content" aria-hidden="true">
      {repeatedWishes.map((text) => (
        <span class="marquee-item">{text}</span>
      ))}
    </div>
  </div>

  <!-- Row 2 -->
  <div class="marquee-track track-2">
    <div class="marquee-content">
      {repeatedWishes.map((text) => (
        <span class="marquee-item">{text}</span>
      ))}
    </div>
    <div class="marquee-content" aria-hidden="true">
      {repeatedWishes.map((text) => (
        <span class="marquee-item">{text}</span>
      ))}
    </div>
  </div>

  <!-- Row 3 -->
  <div class="marquee-track track-3">
    <div class="marquee-content">
      {repeatedWishes.map((text) => (
        <span class="marquee-item">{text}</span>
      ))}
    </div>
    <div class="marquee-content" aria-hidden="true">
      {repeatedWishes.map((text) => (
        <span class="marquee-item">{text}</span>
      ))}
    </div>
  </div>

  <!-- Row 4 -->
  <div class="marquee-track track-4">
    <div class="marquee-content">
      {repeatedWishes.map((text) => (
        <span class="marquee-item">{text}</span>
      ))}
    </div>
    <div class="marquee-content" aria-hidden="true">
      {repeatedWishes.map((text) => (
        <span class="marquee-item">{text}</span>
      ))}
    </div>
  </div>
</div>

<style>
  .marquee-root {
    /* Layout & Positioning */
    position: relative;
    width: 100%;
    overflow: hidden;
    user-select: none;
    pointer-events: none;
    display: flex;
    flex-direction: column;
    gap: 0.25rem; /* More close spacing */
    
    /* Visual styles */
    opacity: 0.1;
  }

  .marquee-track {
    display: flex;
    white-space: nowrap;
    overflow: hidden;
    width: 100%;
  }

  .marquee-content {
    display: flex;
    flex-shrink: 0;
    
    /* Text styles */
    font-size: 1.5rem;
    line-height: 1.25; /* Slightly tighter line height for compactness */
    font-weight: 500;
    letter-spacing: 0.02em; 

    /* Animation */
    animation: marquee-scroll 120s linear infinite;
  }

  /* Different start offsets (negative delay) for each row */
  .track-1 .marquee-content {
    animation-delay: 0s;
    animation-duration: 145s;
  }
  
  .track-2 .marquee-content {
    animation-delay: -20s;
    animation-duration: 200s;
  }
  
  .track-3 .marquee-content {
    animation-delay: -40s;
    animation-duration: 165s;
  }

  .track-4 .marquee-content {
    animation-delay: -60s;
    animation-duration: 185s;
  }

  .marquee-item {
    margin: 0 1.5rem;
  }

  @keyframes marquee-scroll {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-100%);
    }
  }
</style>
